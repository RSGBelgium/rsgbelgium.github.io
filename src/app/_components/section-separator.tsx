/**
 * Section separator (horizontal rule) component.
 *
 * Renders a styled horizontal rule to visually separate sections.
 */

/**
 * Displays a horizontal rule for section separation.
 */
export function SectionSeparator() {
  return <hr className="border-neutral-200 mt-28 mb-24" />;
}
